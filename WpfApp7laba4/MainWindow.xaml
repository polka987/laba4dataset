<Window x:Class="WpfApp7laba4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp7laba4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Name="ProductsButton" Content="Продукты" Background="Honeydew" Click="ProductsButton_Click"/>
        <Button Grid.Column="1" Name="OrdersButton" Content="Заказы" Background="HotPink" Click="OrdersButton_Click" />
        <Button Grid.Column="2" Name="BuyerButton" Content="Покупатели" Background="Honeydew" Click="BuyerButton_Click" />
    </Grid>
</Window>
